# BrainBoost Elite — Windsurf Prompt Engineering Guide
# 6-Phase Build System for Claude Opus 4.6
# Author: John Smith | Role: Apple Principal Engineer ICT Level 7
# Date: February 15, 2026



## HOW TO USE THIS DOCUMENT

Each phase below is a **self-contained prompt** you paste into Windsurf.
- Paste ONE phase at a time
- Wait for full completion and verify before moving to the next
- Each prompt includes full context so Claude knows the entire app vision
- Do NOT skip phases — they build on each other sequentially

---

## GLOBAL CONTEXT (Included in every prompt)

This context block is embedded at the top of each phase prompt so Claude always has full awareness of the project. You do NOT paste this separately — it's already inside each phase.

---

# ═══════════════════════════════════════════════════════════════
# PHASE 1 OF 6 — PROJECT FOUNDATION, DATABASE, & DESIGN SYSTEM
# ═══════════════════════════════════════════════════════════════

Paste this entire block into Windsurf:

```
You are an Apple Principal Engineer ICT Level 7 building "BrainBoost Elite" — an advanced cognitive training app. You will implement this with ZERO shortcuts, ZERO placeholders, ZERO TODO comments. Every file must be production-ready, fully typed, and working.

=== PROJECT OVERVIEW ===

BrainBoost Elite is a cognitive operating system: 6 levels × 6 weeks each = 36-week program. Users do 3 daily sessions (Morning/Afternoon/Evening) scaling from 45 min/day (Level 1) to 90 min/day (Level 6). Features include: spaced repetition, Feynman technique, interleaving, meditation, HIIT, Dual N-Back, trading simulator, AI coach, stress shield (NSDR auto-insert), longevity score, and full gamification.

=== TECH STACK (LOCKED — DO NOT DEVIATE) ===

Frontend:
- Next.js 14 (app router, NOT pages router)
- React 18.2+
- Tailwind CSS 3.4 (custom "Neural Nexus" theme)
- Framer Motion 10 (ALL animations)
- Zustand 4 (global state)
- TanStack React Query 5 (data fetching/mutations to Rust backend)
- Lottie-react 2 (vector animations: sparks, confetti, yoga nidra)
- React Icons 5 (SVG icons: brain, timer, flame, medal)

Backend:
- Rust (latest stable)
- Axum 0.7+ (API framework)
- Tokio (async runtime)
- SQLx 0.7+ (PostgreSQL driver, compile-time checked queries)
- PostgreSQL 16

=== DESIGN SYSTEM — "NEURAL NEXUS" ===

Theme: Dark mode default. Living neural network aesthetic.
Background: #121212 (near-black) with faint gray (#1E1E1E) synaptic grid lines at 45° angles
Colors:
  - Primary Blue: #007BFF (energy, progress, CTAs)
  - Accent Purple: #6F42C1 (creativity, AI coach, premium)
  - Success Green: #28A745 (streaks, completions, recovery)
  - Warning Red: #DC3545 (missed sessions, errors, bias alerts)
  - Orange Accent: #FD7E14 (streak flames)
  - Background: #121212 (main), #1E1E1E (cards/surfaces), #2A2A2A (elevated surfaces)
  - Text: #FFFFFF (primary), #B0B0B0 (secondary), #6C757D (muted)

Typography: Inter font family
  - Body: 16px (1rem)
  - H1: 36px bold
  - H2: 28px bold
  - H3: 24px bold
  - Small/captions: 14px

Buttons: rounded-2xl (16px radius), min h-12 (48px), min w-12 (48px)
  - Hover: shadow-[0_0_8px_rgba(0,123,255,0.5)] + scale-105
  - Active/Press: scale-95
  - Transitions: all 300ms ease-in-out

Cards: rounded-xl (12px), bg-[#1E1E1E], shadow-md, 320px max on mobile
Spacing: 4px base grid (Tailwind default)
Animations: Framer Motion spring physics, 300ms transitions, 60fps target
Accessibility: WCAG 2.1 AA, focus rings (2px blue outline), aria-labels on all interactive elements

=== PHASE 1 TASK: PROJECT SCAFFOLDING, DATABASE SCHEMA, DESIGN SYSTEM ===

Build the complete project foundation. This phase creates the monorepo structure, Rust backend with full database schema, Next.js frontend with the complete design system, and all shared types.

STEP 1: MONOREPO STRUCTURE
Create this exact structure:

brainboost-elite/
├── backend/
│   ├── Cargo.toml
│   ├── .env.example
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── config.rs
│   │   ├── db/
│   │   │   ├── mod.rs
│   │   │   └── pool.rs
│   │   ├── models/
│   │   │   ├── mod.rs
│   │   │   ├── user.rs
│   │   │   ├── progress.rs
│   │   │   ├── exercise.rs
│   │   │   ├── metrics.rs
│   │   │   ├── trading.rs
│   │   │   └── session.rs
│   │   ├── routes/
│   │   │   ├── mod.rs
│   │   │   ├── auth.rs
│   │   │   ├── users.rs
│   │   │   ├── exercises.rs
│   │   │   ├── progress.rs
│   │   │   ├── metrics.rs
│   │   │   ├── trading.rs
│   │   │   ├── coach.rs
│   │   │   └── health.rs
│   │   ├── middleware/
│   │   │   ├── mod.rs
│   │   │   ├── auth.rs
│   │   │   ├── rate_limit.rs
│   │   │   └── cors.rs
│   │   ├── services/
│   │   │   ├── mod.rs
│   │   │   ├── auth_service.rs
│   │   │   ├── exercise_service.rs
│   │   │   ├── progress_service.rs
│   │   │   ├── metrics_service.rs
│   │   │   ├── trading_service.rs
│   │   │   ├── coach_service.rs
│   │   │   └── stress_shield_service.rs
│   │   ├── errors/
│   │   │   └── mod.rs
│   │   └── utils/
│   │       ├── mod.rs
│   │       ├── jwt.rs
│   │       └── validators.rs
│   └── migrations/
│       └── (SQLx migration files)
├── frontend/
│   ├── package.json
│   ├── next.config.js
│   ├── tailwind.config.ts
│   ├── tsconfig.json
│   ├── postcss.config.js
│   ├── public/
│   │   ├── manifest.json
│   │   ├── icons/
│   │   └── animations/ (Lottie JSON files)
│   ├── src/
│   │   ├── app/
│   │   │   ├── layout.tsx (root layout with providers, Inter font, dark theme)
│   │   │   ├── page.tsx (landing/redirect)
│   │   │   ├── globals.css
│   │   │   ├── (auth)/
│   │   │   │   ├── login/page.tsx
│   │   │   │   ├── register/page.tsx
│   │   │   │   └── layout.tsx
│   │   │   ├── (app)/
│   │   │   │   ├── layout.tsx (authenticated layout with bottom nav)
│   │   │   │   ├── dashboard/page.tsx
│   │   │   │   ├── levels/page.tsx
│   │   │   │   ├── levels/[levelId]/page.tsx
│   │   │   │   ├── exercise/[sessionId]/page.tsx
│   │   │   │   ├── coach/page.tsx
│   │   │   │   ├── trading/page.tsx
│   │   │   │   ├── community/page.tsx
│   │   │   │   └── profile/page.tsx
│   │   │   └── onboarding/
│   │   │       └── page.tsx
│   │   ├── components/
│   │   │   ├── ui/ (design system atoms)
│   │   │   │   ├── Button.tsx
│   │   │   │   ├── Card.tsx
│   │   │   │   ├── Input.tsx
│   │   │   │   ├── Modal.tsx
│   │   │   │   ├── Toast.tsx
│   │   │   │   ├── Badge.tsx
│   │   │   │   ├── Toggle.tsx
│   │   │   │   ├── Slider.tsx
│   │   │   │   ├── ProgressBar.tsx
│   │   │   │   ├── Accordion.tsx
│   │   │   │   └── Tooltip.tsx
│   │   │   ├── layout/
│   │   │   │   ├── BottomNav.tsx
│   │   │   │   ├── TopBar.tsx
│   │   │   │   └── NeuralBackground.tsx
│   │   │   ├── dashboard/
│   │   │   │   ├── BrainScoreGauge.tsx
│   │   │   │   ├── ReadinessGauge.tsx
│   │   │   │   ├── DailyChecklist.tsx
│   │   │   │   ├── StreakFlame.tsx
│   │   │   │   ├── QuickStartButton.tsx
│   │   │   │   └── MetricsOverview.tsx
│   │   │   ├── exercise/
│   │   │   │   ├── TimerCircle.tsx
│   │   │   │   ├── ExerciseModal.tsx
│   │   │   │   ├── MeditationPlayer.tsx
│   │   │   │   ├── FlashcardDeck.tsx
│   │   │   │   ├── FeynmanRecorder.tsx
│   │   │   │   ├── PuzzleGame.tsx
│   │   │   │   ├── DualNBack.tsx
│   │   │   │   ├── SpeedDrill.tsx
│   │   │   │   └── ExerciseComplete.tsx
│   │   │   ├── trading/
│   │   │   │   ├── CandlestickChart.tsx
│   │   │   │   ├── BiasAlert.tsx
│   │   │   │   ├── TradingScenario.tsx
│   │   │   │   ├── DecisionPanel.tsx
│   │   │   │   └── TradingLeaderboard.tsx
│   │   │   ├── coach/
│   │   │   │   ├── ChatBubble.tsx
│   │   │   │   ├── CoachInput.tsx
│   │   │   │   ├── SuggestionCarousel.tsx
│   │   │   │   └── NSDRModal.tsx
│   │   │   ├── levels/
│   │   │   │   ├── LevelTimeline.tsx
│   │   │   │   ├── LevelCard.tsx
│   │   │   │   ├── WeeklyTabs.tsx
│   │   │   │   └── LevelUnlockAnimation.tsx
│   │   │   ├── onboarding/
│   │   │   │   ├── OnboardingCarousel.tsx
│   │   │   │   ├── ChronotypeSlider.tsx
│   │   │   │   ├── GoalPicker.tsx
│   │   │   │   └── WelcomeHologram.tsx
│   │   │   ├── profile/
│   │   │   │   ├── StatsCharts.tsx
│   │   │   │   ├── AvatarEditor.tsx
│   │   │   │   └── SettingsPanel.tsx
│   │   │   └── shared/
│   │   │       ├── ConfettiEffect.tsx
│   │   │       ├── SparkEffect.tsx
│   │   │       ├── LoadingSpinner.tsx
│   │   │       └── ErrorBoundary.tsx
│   │   ├── hooks/
│   │   │   ├── useAuth.ts
│   │   │   ├── useProgress.ts
│   │   │   ├── useExercise.ts
│   │   │   ├── useTimer.ts
│   │   │   ├── useMetrics.ts
│   │   │   ├── useTrading.ts
│   │   │   ├── useCoach.ts
│   │   │   ├── useHaptic.ts
│   │   │   └── useReadiness.ts
│   │   ├── stores/
│   │   │   ├── authStore.ts
│   │   │   ├── progressStore.ts
│   │   │   ├── exerciseStore.ts
│   │   │   ├── metricsStore.ts
│   │   │   └── uiStore.ts
│   │   ├── lib/
│   │   │   ├── api.ts (Axios/fetch wrapper for Rust backend)
│   │   │   ├── queryClient.ts (TanStack Query config)
│   │   │   ├── constants.ts
│   │   │   └── utils.ts
│   │   └── types/
│   │       ├── user.ts
│   │       ├── exercise.ts
│   │       ├── progress.ts
│   │       ├── metrics.ts
│   │       ├── trading.ts
│   │       └── api.ts
│   └── next-env.d.ts
├── shared/
│   └── types/ (shared TypeScript types if needed)
├── docker-compose.yml (PostgreSQL + backend + frontend)
├── .gitignore
└── README.md

STEP 2: BACKEND — Cargo.toml & Dependencies
Create Cargo.toml with these exact dependencies:

[package]
name = "brainboost-elite-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["macros", "ws"] }
axum-extra = { version = "0.9", features = ["typed-header"] }
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "postgres", "chrono", "uuid", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
jsonwebtoken = "9"
bcrypt = "0.15"
tower = { version = "0.4", features = ["full"] }
tower-http = { version = "0.5", features = ["cors", "trace", "limit"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
dotenvy = "0.15"
thiserror = "1"
validator = { version = "0.18", features = ["derive"] }
rand = "0.8"

STEP 3: DATABASE SCHEMA — Complete PostgreSQL migrations
Create SQLx migrations for the COMPLETE schema. Every table, every index, every constraint.

Migration 001 — Core tables:

-- Users table
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

CREATE TYPE subscription_tier AS ENUM ('free', 'premium');
CREATE TYPE user_status AS ENUM ('active', 'suspended', 'deleted');

CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    display_name VARCHAR(100) NOT NULL,
    avatar_url TEXT,
    subscription subscription_tier NOT NULL DEFAULT 'free',
    status user_status NOT NULL DEFAULT 'active',
    chronotype VARCHAR(20), -- 'early_bird', 'night_owl', 'flexible'
    primary_goal VARCHAR(50), -- 'rapid_learning', 'trading_mastery', 'stress_proof', 'max_neural'
    timezone VARCHAR(50) NOT NULL DEFAULT 'America/New_York',
    onboarding_completed BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Session schedule preferences
CREATE TABLE user_schedule (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    morning_time TIME NOT NULL DEFAULT '07:30',
    afternoon_time TIME NOT NULL DEFAULT '13:30',
    evening_time TIME NOT NULL DEFAULT '20:30',
    notification_enabled BOOLEAN NOT NULL DEFAULT true,
    haptic_enabled BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id)
);

-- Level progress tracking
CREATE TYPE level_status AS ENUM ('locked', 'active', 'completed');

CREATE TABLE user_levels (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    level_number SMALLINT NOT NULL CHECK (level_number BETWEEN 1 AND 6),
    status level_status NOT NULL DEFAULT 'locked',
    current_week SMALLINT NOT NULL DEFAULT 1 CHECK (current_week BETWEEN 1 AND 6),
    current_day SMALLINT NOT NULL DEFAULT 1 CHECK (current_day BETWEEN 1 AND 7),
    grace_skip_used BOOLEAN NOT NULL DEFAULT false,
    compliance_rate DECIMAL(5,2) NOT NULL DEFAULT 0.00,
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    restarted_count INT NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, level_number)
);

-- Daily session tracking
CREATE TYPE session_slot AS ENUM ('morning', 'afternoon', 'evening');
CREATE TYPE session_status AS ENUM ('pending', 'in_progress', 'completed', 'skipped', 'missed');

CREATE TABLE daily_sessions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    level_number SMALLINT NOT NULL,
    week_number SMALLINT NOT NULL,
    day_number SMALLINT NOT NULL,
    day_date DATE NOT NULL,
    slot session_slot NOT NULL,
    status session_status NOT NULL DEFAULT 'pending',
    scheduled_time TIME,
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    duration_planned_seconds INT NOT NULL,
    duration_actual_seconds INT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, day_date, slot)
);

-- Individual exercise completions within sessions
CREATE TYPE exercise_type AS ENUM (
    'meditation', 'body_scan', 'nsdr', 'visualization', 'qigong',
    'diet_log', 'sleep_prep', 'sleep_log',
    'retrieval_practice', 'spaced_repetition', 'interleaving',
    'feynman_technique', 'reading', 'speed_drill',
    'puzzle', 'dual_nback', 'bias_busting',
    'exercise_light', 'exercise_brisk', 'hiit', 'cold_exposure',
    'new_skill', 'creative_skill',
    'cbt_reframe', 'journaling', 'distraction_setup',
    'trading_sim', 'rl_trading', 'custom_combo'
);

CREATE TABLE exercise_completions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    session_id UUID NOT NULL REFERENCES daily_sessions(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    exercise_type exercise_type NOT NULL,
    exercise_name VARCHAR(200) NOT NULL,
    duration_seconds INT NOT NULL,
    accuracy_score DECIMAL(5,2), -- 0-100 percentage
    focus_rating SMALLINT CHECK (focus_rating BETWEEN 1 AND 10),
    difficulty_level SMALLINT,
    metadata JSONB DEFAULT '{}', -- flexible data per exercise type
    completed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Metrics snapshots (daily aggregated)
CREATE TABLE daily_metrics (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    metric_date DATE NOT NULL,
    brain_score DECIMAL(5,2), -- 0-100 (40% completion + 30% accuracy + 20% focus + 10% stress_low)
    stress_index DECIMAL(5,2), -- 0-100 (lower is better)
    longevity_score DECIMAL(5,2), -- 0-100
    completion_rate DECIMAL(5,2), -- percentage of exercises completed
    avg_accuracy DECIMAL(5,2),
    avg_focus DECIMAL(5,2),
    total_session_minutes INT,
    streak_days INT NOT NULL DEFAULT 0,
    hrv_reading DECIMAL(6,2), -- from wearable if available
    sleep_hours DECIMAL(4,2),
    sleep_quality SMALLINT CHECK (sleep_quality BETWEEN 1 AND 10),
    exercise_minutes INT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, metric_date)
);

-- Spaced repetition flashcards
CREATE TABLE flashcards (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    topic VARCHAR(200) NOT NULL,
    question TEXT NOT NULL,
    answer TEXT NOT NULL,
    difficulty SMALLINT NOT NULL DEFAULT 1 CHECK (difficulty BETWEEN 1 AND 5),
    ease_factor DECIMAL(4,2) NOT NULL DEFAULT 2.50,
    interval_days INT NOT NULL DEFAULT 1,
    repetitions INT NOT NULL DEFAULT 0,
    next_review_date DATE NOT NULL DEFAULT CURRENT_DATE,
    last_reviewed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Hyper-Learning topics
CREATE TABLE learning_topics (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    topic_name VARCHAR(200) NOT NULL,
    description TEXT,
    total_micro_lessons INT NOT NULL DEFAULT 0,
    completed_micro_lessons INT NOT NULL DEFAULT 0,
    estimated_mastery_days INT,
    mastery_percentage DECIMAL(5,2) NOT NULL DEFAULT 0.00,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE micro_lessons (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    topic_id UUID NOT NULL REFERENCES learning_topics(id) ON DELETE CASCADE,
    lesson_order SMALLINT NOT NULL,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    duration_minutes SMALLINT NOT NULL DEFAULT 10,
    next_scheduled_at TIMESTAMPTZ,
    completed BOOLEAN NOT NULL DEFAULT false,
    score DECIMAL(5,2),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Trading simulator data
CREATE TABLE trading_sessions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    scenario_type VARCHAR(50) NOT NULL, -- 'bias_busting', 'rl_trading', 'market_sim'
    starting_balance DECIMAL(12,2) NOT NULL DEFAULT 100000.00,
    ending_balance DECIMAL(12,2),
    total_trades INT NOT NULL DEFAULT 0,
    winning_trades INT NOT NULL DEFAULT 0,
    expected_value_score DECIMAL(8,4),
    bias_detections JSONB DEFAULT '[]',
    process_score DECIMAL(5,2), -- adherence to trading rules
    decision_speed_ms INT, -- average decision time
    started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE trading_decisions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    trading_session_id UUID NOT NULL REFERENCES trading_sessions(id) ON DELETE CASCADE,
    decision_type VARCHAR(20) NOT NULL, -- 'buy', 'sell', 'hold'
    asset_symbol VARCHAR(20) NOT NULL,
    price_at_decision DECIMAL(12,4) NOT NULL,
    quantity DECIMAL(12,4),
    reasoning TEXT,
    ai_feedback TEXT,
    bias_detected VARCHAR(50)[], -- array of detected biases
    was_optimal BOOLEAN,
    pnl DECIMAL(12,2),
    decision_time_ms INT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- AI Coach conversation history
CREATE TABLE coach_messages (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL, -- 'user', 'coach', 'system'
    content TEXT NOT NULL,
    suggestion_type VARCHAR(50), -- 'nsdr_insert', 'schedule_adjust', 'technique_tip', etc.
    metadata JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Streaks and achievements
CREATE TABLE achievements (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(100) NOT NULL UNIQUE,
    description TEXT NOT NULL,
    icon_name VARCHAR(50) NOT NULL,
    category VARCHAR(50) NOT NULL, -- 'streak', 'mastery', 'trading', 'wellness', 'level'
    requirement_value INT NOT NULL,
    requirement_type VARCHAR(50) NOT NULL -- 'streak_days', 'brain_score', 'trades_won', etc.
);

CREATE TABLE user_achievements (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    achievement_id UUID NOT NULL REFERENCES achievements(id) ON DELETE CASCADE,
    unlocked_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, achievement_id)
);

-- Refresh tokens for JWT
CREATE TABLE refresh_tokens (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token_hash VARCHAR(255) NOT NULL,
    expires_at TIMESTAMPTZ NOT NULL,
    revoked BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Indexes for performance
CREATE INDEX idx_daily_sessions_user_date ON daily_sessions(user_id, day_date);
CREATE INDEX idx_daily_sessions_status ON daily_sessions(user_id, status);
CREATE INDEX idx_exercise_completions_session ON exercise_completions(session_id);
CREATE INDEX idx_exercise_completions_user ON exercise_completions(user_id, completed_at);
CREATE INDEX idx_daily_metrics_user_date ON daily_metrics(user_id, metric_date);
CREATE INDEX idx_flashcards_review ON flashcards(user_id, next_review_date);
CREATE INDEX idx_trading_sessions_user ON trading_sessions(user_id, created_at);
CREATE INDEX idx_coach_messages_user ON coach_messages(user_id, created_at);
CREATE INDEX idx_user_levels_user ON user_levels(user_id, level_number);
CREATE INDEX idx_refresh_tokens_user ON refresh_tokens(user_id, revoked);

Migration 002 — Seed data (achievements):

INSERT INTO achievements (id, name, description, icon_name, category, requirement_value, requirement_type) VALUES
(uuid_generate_v4(), 'First Spark', 'Complete your first exercise session', 'zap', 'level', 1, 'sessions_completed'),
(uuid_generate_v4(), 'Week Warrior', 'Complete 7 consecutive days', 'flame', 'streak', 7, 'streak_days'),
(uuid_generate_v4(), 'Neural Ignition Complete', 'Graduate Level 1', 'brain', 'level', 1, 'level_completed'),
(uuid_generate_v4(), 'Synaptic Surge Complete', 'Graduate Level 2', 'brain', 'level', 2, 'level_completed'),
(uuid_generate_v4(), 'Month Master', 'Maintain a 30-day streak', 'trophy', 'streak', 30, 'streak_days'),
(uuid_generate_v4(), 'Recall Champion', 'Achieve 95% accuracy on flashcards', 'target', 'mastery', 95, 'flashcard_accuracy'),
(uuid_generate_v4(), 'Trading Rookie', 'Complete 10 trading simulations', 'trending-up', 'trading', 10, 'trading_sessions'),
(uuid_generate_v4(), 'Bias Buster', 'Detect and correct 50 biases', 'shield', 'trading', 50, 'biases_corrected'),
(uuid_generate_v4(), 'Zen Mind', 'Complete 100 meditation sessions', 'heart', 'wellness', 100, 'meditation_sessions'),
(uuid_generate_v4(), 'Iron Streak', 'Maintain a 100-day streak', 'award', 'streak', 100, 'streak_days'),
(uuid_generate_v4(), 'Transhuman', 'Reach Level 6', 'star', 'level', 6, 'level_completed'),
(uuid_generate_v4(), 'Speed Demon', 'Score 90%+ on speed processing drills', 'zap', 'mastery', 90, 'speed_drill_score'),
(uuid_generate_v4(), 'Feynman Master', 'Complete 50 Feynman Technique sessions', 'book-open', 'mastery', 50, 'feynman_sessions'),
(uuid_generate_v4(), 'Cold Warrior', 'Complete 30 cold exposure sessions', 'snowflake', 'wellness', 30, 'cold_sessions'),
(uuid_generate_v4(), 'Brain Score 90', 'Achieve a Brain Score of 90+', 'award', 'mastery', 90, 'brain_score');

STEP 4: RUST BACKEND — Core server setup
Implement the complete Rust backend foundation:

main.rs:
- Load .env config
- Initialize tracing (structured JSON logging)
- Create PostgreSQL connection pool (SQLx)
- Run pending migrations on startup
- Build Axum router with all route groups
- Add middleware: CORS (allow frontend origin), rate limiting (tower), request tracing
- Start server on configurable port (default 8080)
- Graceful shutdown on SIGTERM

config.rs:
- Struct with all env vars: DATABASE_URL, JWT_SECRET, JWT_EXPIRY_HOURS, FRONTEND_URL, PORT, RUST_LOG
- Lazy static or once_cell initialization
- Validation on startup (panic if missing required vars)

db/pool.rs:
- Create PgPool with max_connections=20, min_connections=5, acquire_timeout=30s
- Health check query on init

errors/mod.rs:
- Custom AppError enum with variants: NotFound, Unauthorized, Forbidden, ValidationError, DatabaseError, InternalError, Conflict, RateLimited
- Implement IntoResponse for Axum (return proper JSON error bodies with status codes)
- Implement From<sqlx::Error>, From<jsonwebtoken::errors::Error>, From<bcrypt::BcryptError>

models/ — All models as Rust structs with:
- #[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)] where applicable
- Separate CreateRequest and UpdateRequest structs with #[derive(Deserialize, Validate)]
- Response structs that exclude sensitive fields (no password_hash)

user.rs models:
  - User (full DB row)
  - UserResponse (id, email, display_name, avatar_url, subscription, chronotype, primary_goal, onboarding_completed, created_at)
  - CreateUserRequest (email, password, display_name) with validation: email format, password min 8 chars
  - LoginRequest (email, password)
  - AuthResponse (user: UserResponse, access_token, refresh_token, expires_at)
  - UpdateProfileRequest (display_name?, avatar_url?, chronotype?, primary_goal?, timezone?)

progress.rs models:
  - UserLevel (full DB row)
  - UserLevelResponse
  - DailySession (full DB row)
  - DailySessionResponse
  - StartSessionRequest
  - CompleteSessionRequest

exercise.rs models:
  - ExerciseCompletion
  - CompleteExerciseRequest (exercise_type, duration_seconds, accuracy_score?, focus_rating?, metadata?)
  - ExerciseHistoryResponse

metrics.rs models:
  - DailyMetrics
  - MetricsResponse (includes brain_score, stress_index, longevity_score, streak)
  - MetricsTrendResponse (array of daily snapshots for charts)

trading.rs models:
  - TradingSession, TradingDecision
  - StartTradingRequest, MakeDecisionRequest
  - TradingSessionResponse, TradingStatsResponse

session.rs models:
  - Flashcard, LearningTopic, MicroLesson
  - CreateFlashcardRequest, ReviewFlashcardRequest
  - CreateTopicRequest

utils/jwt.rs:
- encode_access_token(user_id: Uuid) -> Result<String> (expires in config hours, RS256 or HS256)
- encode_refresh_token(user_id: Uuid) -> Result<String> (expires 7 days)
- decode_token(token: &str) -> Result<Claims>
- Claims struct: sub (user_id as string), exp, iat

middleware/auth.rs:
- Extract Bearer token from Authorization header
- Decode and validate JWT
- Inject user_id into request extensions
- Return 401 on failure with JSON body

middleware/rate_limit.rs:
- Tower-based rate limiter: 60 requests per minute per IP for general routes
- 10 requests per minute for auth routes (login/register)

middleware/cors.rs:
- Allow origin from FRONTEND_URL env var
- Allow methods: GET, POST, PUT, PATCH, DELETE, OPTIONS
- Allow headers: Authorization, Content-Type
- Allow credentials: true

routes/ — Define ALL route handlers as stubs that return proper response structures:
- auth.rs: POST /auth/register, POST /auth/login, POST /auth/refresh, POST /auth/logout
- users.rs: GET /users/me, PATCH /users/me, GET /users/me/schedule, PUT /users/me/schedule
- progress.rs: GET /progress/current (current level/week/day), GET /progress/levels, POST /progress/advance-day
- exercises.rs: GET /exercises/today (today's exercise plan), POST /exercises/complete, GET /exercises/history
- metrics.rs: GET /metrics/today, GET /metrics/trend?days=30, GET /metrics/brain-score
- trading.rs: POST /trading/start, POST /trading/decide, GET /trading/history, GET /trading/stats
- coach.rs: POST /coach/message, GET /coach/suggestions, GET /coach/history
- health.rs: GET /health (returns {"status":"ok","db":"connected"})

STEP 5: FRONTEND — Next.js setup with complete design system

package.json dependencies (exact versions):
{
  "next": "14.2.0",
  "react": "18.2.0",
  "react-dom": "18.2.0",
  "tailwindcss": "3.4.0",
  "framer-motion": "10.18.0",
  "zustand": "4.5.0",
  "@tanstack/react-query": "5.20.0",
  "lottie-react": "2.4.0",
  "react-icons": "5.0.0",
  "axios": "1.6.0",
  "clsx": "2.1.0",
  "date-fns": "3.3.0"
}

tailwind.config.ts — COMPLETE custom Neural Nexus theme:
- extend colors: neuralBlue, creativePurple, successGreen, warningRed, orangeAccent, surface (DEFAULT, card, elevated), textPrimary, textSecondary, textMuted
- extend boxShadow: glow-blue, glow-purple, glow-green
- extend animation: pulse-glow, synaptic-fire, streak-burn, shake-error
- extend keyframes for each custom animation
- fontFamily: { sans: ['Inter', 'system-ui', 'sans-serif'] }

globals.css:
- Import Tailwind directives
- Set html/body to bg-[#121212] text-white
- Custom scrollbar styling (thin, dark)
- Selection color (blue tint)

Root layout.tsx:
- Import Inter from next/font/google
- Wrap with QueryClientProvider (TanStack)
- Apply dark class to html
- Set metadata: title "BrainBoost Elite", description, theme-color #121212

lib/api.ts:
- Axios instance with baseURL from env (NEXT_PUBLIC_API_URL)
- Request interceptor: attach access_token from authStore
- Response interceptor: on 401, attempt token refresh, retry, or redirect to login
- Type-safe wrapper functions for all API endpoints

lib/queryClient.ts:
- Default staleTime: 5 minutes
- Default retry: 2
- RefetchOnWindowFocus: false

All Zustand stores with proper TypeScript:
- authStore: user, accessToken, refreshToken, isAuthenticated, login(), logout(), refreshAuth()
- progressStore: currentLevel, currentWeek, currentDay, compliance, streak, todaySessions, setProgress(), advanceDay()
- exerciseStore: activeExercise, timerState, isSessionActive, startExercise(), completeExercise(), pauseTimer()
- metricsStore: brainScore, stressIndex, longevityScore, trendData, fetchMetrics()
- uiStore: theme, toasts, modals, sidebarOpen, addToast(), removeToast(), toggleTheme()

All TypeScript types matching the Rust models exactly (types/ directory).

All UI components (components/ui/) fully implemented with:
- Full Tailwind styling matching Neural Nexus design system
- Framer Motion animations (hover, tap, enter/exit)
- Proper TypeScript props interfaces
- aria-labels and accessibility attributes
- forwardRef where appropriate

Button.tsx variants: primary (blue), secondary (purple), success (green), danger (red), ghost (transparent), sizes: sm/md/lg
Card.tsx: dark surface bg, rounded-xl, shadow, optional expandable accordion mode
Modal.tsx: Full-screen overlay with dimmed bg, centered card, Framer AnimatePresence for enter/exit
Toast.tsx: Slide-up notification with auto-dismiss, variants: success/error/warning/info
ProgressBar.tsx: Linear bar with gradient fill and optional particle animation
Input.tsx: Dark themed input with focus ring blue, error state red, label above

NeuralBackground.tsx:
- SVG-based animated background with faint gray lines at 45° angles
- Subtle pulse animation on the lines (opacity oscillation 0.03-0.08)
- Performance: Use CSS animations, NOT JS-driven (battery efficient)
- Only renders on dashboard and main app screens

BottomNav.tsx:
- Fixed bottom, h-16 (64px), bg-surface with top border
- 5 items: Dashboard (grid icon), Levels (layers icon), Coach (message icon), Community (users icon), Profile (user icon)
- Active item: neuralBlue color + scale-110 + glow
- Framer layout animation on active indicator

TopBar.tsx:
- Fixed top, h-12 (48px), bg-surface/80 backdrop-blur
- Left: Brain icon (pulsing blue SVG)
- Center: "BrainBoost Elite" text
- Right: User avatar (32px circle with purple ring), progress wheel overlay

docker-compose.yml:
- PostgreSQL 16 service (port 5432, with volume for data persistence)
- Environment variables template

CRITICAL REQUIREMENTS:
1. Every file must compile/build without errors
2. Every component must be fully implemented (no placeholder returns)
3. All Rust models must match the SQL schema exactly
4. All TypeScript types must match the Rust response types
5. All UI components must have proper Framer Motion animations
6. Tailwind config must include ALL custom Neural Nexus values
7. API client must have typed wrappers for every backend endpoint
8. Zustand stores must be fully typed and functional
9. The app must start with `docker compose up` (Postgres), `cargo run` (backend), and `npm run dev` (frontend)

Build everything now. Start with the backend Cargo.toml and main.rs, then migrations, then models, then routes, then frontend package.json, then tailwind config, then all components in order. Do not stop until every single file listed above is created and working.
```

---
