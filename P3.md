# ═══════════════════════════════════════════════════════════════
# PHASE 3 OF 6 — COMPLETE FRONTEND UI: ALL SCREENS & COMPONENTS
# ═══════════════════════════════════════════════════════════════

Paste this entire block into Windsurf:

```
You are continuing to build "BrainBoost Elite" as an Apple Principal Engineer ICT Level 7. Phase 1 (scaffolding/schema) and Phase 2 (auth/services/exercise engine) are complete. Now build EVERY frontend screen and component with pixel-perfect Neural Nexus design.

CRITICAL RULES:
- Every component must use Framer Motion for animations (enter/exit/hover/tap)
- Every component must be fully responsive (mobile-first, then desktop)
- Every interactive element must have aria-labels and keyboard support
- Every component must use the Neural Nexus color palette from Tailwind config
- NO placeholder content — use realistic mock data where API isn't connected yet
- ALL components must be TypeScript with proper prop interfaces

=== DESIGN SYSTEM RECAP ===
Background: #121212 | Cards: #1E1E1E | Elevated: #2A2A2A
Blue: #007BFF | Purple: #6F42C1 | Green: #28A745 | Red: #DC3545 | Orange: #FD7E14
Text: #FFFFFF (primary), #B0B0B0 (secondary), #6C757D (muted)
Font: Inter | Body: 16px | H1: 36px | H2: 28px | H3: 24px
Buttons: rounded-2xl, h-12 min, hover glow + scale-105, press scale-95, 300ms transitions
Cards: rounded-xl, shadow-md, bg-[#1E1E1E]

=== SCREEN 1: ONBOARDING (5-screen carousel) ===

File: src/app/onboarding/page.tsx + components/onboarding/*

OnboardingCarousel.tsx:
- Full-screen carousel with swipe support (touch + mouse drag via Framer Motion)
- Dot indicators at bottom (8px circles, active = neuralBlue filled, inactive = gray-600 ring)
- "Next" button bottom-right (primary blue, 48px height)
- "Skip" link top-right (text-muted, underline on hover)
- AnimatePresence for smooth slide transitions (slide from right, exit to left)

Screen 1 — WelcomeHologram.tsx:
- Full-screen gradient background (linear from neuralBlue at top to creativePurple at bottom, opacity 0.3 over #121212)
- Center: Animated brain SVG (create a simple brain outline in SVG, 200px, pulsing glow animation: scale 1.0-1.05 with blue shadow, 2s infinite ease-in-out)
- Text below brain: "Unlock Your Super Brain" in 32px bold white, fade-in from opacity 0 (delay 0.5s)
- Subtext: "36 weeks to genius-level cognitive performance" in 18px text-secondary, fade-in delay 0.8s
- "Get Started" button: full-width, h-14, neuralBlue bg, white text, bold, rounded-2xl, glow shadow on hover, scale-95 on tap

Screen 2 — ChronotypeSlider.tsx:
- Question: "When do you feel most energized?" in 24px bold
- Three visual options as large tappable cards (160x120px each, horizontal row):
  - "Early Bird" (sunrise icon, orange tint) — morning_time defaults 6:30
  - "Flexible" (sun icon, blue tint) — morning_time defaults 8:00
  - "Night Owl" (moon icon, purple tint) — morning_time defaults 9:30
- Selected card: scale-105, blue border-2, glow shadow
- Unselected: border-gray-700, opacity 0.6
- Time adjusters below: 3 time pickers (Morning/Afternoon/Evening) with +/- 30min buttons
- Framer: cards spring on tap, selected pops up

Screen 3 — GoalPicker.tsx:
- Question: "What's your primary super-brain goal?" in 24px bold
- 4 cards in 2x2 grid (each ~150x150px on mobile, 200x180px on desktop):
  1. "Rapid Learning" — book icon, blue gradient top border, description: "Compress months into days"
  2. "Trading Mastery" — trending-up icon, green gradient, description: "Elite decision-making"
  3. "Stress-Proof" — shield icon, purple gradient, description: "Calm under any pressure"
  4. "Max Neural" — zap icon, orange gradient, description: "Maximum cognitive power"
- Cards tilt 3-5° on hover (Framer rotateY), selected: solid border + checkmark badge top-right
- Multi-select allowed (but primary = first selected)

Screen 4 — Experience level:
- Question: "How experienced are you with cognitive training?" in 24px bold
- 3 horizontal options: "Beginner" (plant icon), "Intermediate" (tree icon), "Advanced" (brain icon)
- Same card selection pattern as chronotype

Screen 5 — Summary + Start:
- "Your Personalized Plan" heading
- Summary card showing: Chronotype, Goal, Starting Level (always 1), Daily time commitment (45 min)
- Schedule preview: Morning @ {time}, Afternoon @ {time}, Evening @ {time}
- "Begin Your Journey" button: full-width, h-14, gradient blue-to-purple bg, white bold text, confetti Lottie plays on tap (blue sparks, 2s)
- On tap: POST /api/users/me with onboarding data, set onboarding_completed=true, navigate to /dashboard

=== SCREEN 2: DASHBOARD ===

File: src/app/(app)/dashboard/page.tsx + components/dashboard/*

Layout: Single column mobile, 2-column grid on desktop (main left, sidebar right)

NeuralBackground.tsx (behind dashboard):
- SVG with ~20 thin lines at various 45° angles, stroke #1E1E1E, opacity 0.05-0.1
- CSS keyframe: each line fades between opacity 0.03 and 0.08 at different rates (3-8s)
- When an exercise is completed, nearby lines briefly flash to opacity 0.3 (blue tint) for 1s — "synaptic fire" effect
- Absolutely positioned behind all content, z-0

BrainScoreGauge.tsx:
- Circular SVG gauge, 120px diameter
- Background circle: stroke #2A2A2A, stroke-width 8
- Progress arc: stroke neuralBlue, stroke-width 8, stroke-linecap round
- Arc fills clockwise based on brainScore (0-100)
- Center text: score number in 32px bold white + "/100" in 14px text-muted below
- Label below circle: "Brain Score" in 14px text-secondary
- Framer: arc animates from 0 to value on mount (spring, duration 1.5s)
- Spark particles (3-5 tiny blue dots) emit from the arc tip during animation using Framer variants
- Update animation: old value morphs to new value when data changes

ReadinessGauge.tsx:
- Horizontal bar, 100% width of container, h-3 (12px), rounded-full
- Background: #2A2A2A
- Fill: gradient from green (#28A745) at 100% to yellow (#FFC107) at 50% to red (#DC3545) at 0%
- Fill width = readiness percentage (derived from stress_index: readiness = 100 - stress_index)
- If readiness < 40: bar pulses red (Framer animate opacity 0.7-1.0, repeat infinite, 1.5s)
- Label left: "Readiness" in 14px text-secondary
- Label right: "{value}%" in 14px, colored by range (green/yellow/red)
- Below bar if readiness < 40: warning text "Low readiness detected — NSDR recommended" in warningRed, 12px

DailyChecklist.tsx:
- 3 accordion cards (one per slot: Morning, Afternoon, Evening)
- Each card: bg-[#1E1E1E], rounded-xl, mb-3, overflow hidden
- Card header (always visible): slot icon (sunrise/sun/moon) + slot name + time + status badge
  - Status badge: "Pending" (gray), "In Progress" (blue pulse), "Completed" (green check), "Missed" (red x)
  - Chevron right side, rotates 180° when expanded
- Card body (expanded): list of exercises for that slot
  - Each exercise row: checkbox (custom styled circle, 24px) + name + duration + optional accuracy badge
  - Completed exercises: green check, text line-through effect, opacity 0.7
  - Active exercise: blue left border (3px), bg slightly lighter
  - Tapping an uncompleted exercise opens the ExerciseModal
- Framer: accordion expand/collapse with layoutId for smooth height morph (300ms spring)
- Checkmark animation: scale from 0 to 1.2 to 1.0 with bounce (spring, stiffness 300)

StreakFlame.tsx:
- Fire icon (from React Icons: FaFire) colored with gradient orange-to-red
- Size: base 24px, scales up by 0.1 per consecutive day (max 2x at 10+ days)
- Number overlay: streak count in bold white, 14px, positioned bottom-right
- Glow: orange box-shadow that intensifies with streak length
- Framer: constant subtle dance animation (rotate -3° to 3°, scale 0.95-1.05, repeat infinite, 2s)
- At milestones (7, 30, 100 days): flame briefly turns blue with burst animation

QuickStartButton.tsx:
- Floating circular button, 56px, positioned bottom-center above BottomNav
- Background: neuralBlue gradient (radial from center lighter to edge darker)
- Icon: play triangle (white, 20px)
- Shadow: glow-blue (0 0 12px rgba(0,123,255,0.6))
- Framer: on tap, rotates 360° (spring, 0.5s) then navigates to next pending exercise
- Disabled state (all done): green bg, checkmark icon, "All Done!" tooltip

MetricsOverview.tsx:
- Row of 4 mini metric cards (horizontal scroll on mobile, grid on desktop)
- Each card: 80x80px, bg-elevated, rounded-lg, centered content
  1. Brain Score: gauge icon, score number, neuralBlue
  2. Streak: flame icon, day count, orange
  3. Stress: heart icon, index number, color by range
  4. Longevity: shield icon, score, green
- Framer: stagger-in from bottom (each card delays +0.1s)

=== SCREEN 3: EXERCISE INTERFACE ===

File: src/app/(app)/exercise/[sessionId]/page.tsx + components/exercise/*

ExerciseModal.tsx:
- Full-screen overlay: bg-black/60 backdrop-blur-sm, z-50
- Center card: bg-[#1E1E1E], w-[90%] max-w-lg, rounded-2xl, p-6
- AnimatePresence: slide up from bottom + fade in (300ms spring)
- Exit: slide down + fade out
- Close button: top-right, X icon, 32px, ghost button
- Content layout (top to bottom):
  1. Exercise icon + name (24px bold)
  2. Duration badge (e.g., "10 min", bg-neuralBlue/20, text-neuralBlue, rounded-full, px-3)
  3. Difficulty dots (1-10 filled/unfilled circles, 8px each)
  4. Instructions text (16px, text-secondary, leading-relaxed)
  5. TimerCircle (centered, 160px)
  6. Action button: "Start" → "Pause" → "Resume" / "Complete"

TimerCircle.tsx:
- SVG circle, 160px diameter
- Outer ring: stroke #2A2A2A, width 6
- Progress ring: stroke neuralBlue, width 6, dasharray for progress, linecap round
- Ring depletes clockwise (full → empty as time runs out)
- Center: remaining time in 36px bold white (MM:SS format)
- Below time: current exercise name in 12px text-muted
- Framer: ring animates smoothly (update every frame via RAF)
- At 50%: ring color shifts to orange (#FFC107), haptic buzz (medium)
- At 10%: ring color shifts to red, pulses
- At 0%: ring fills green, haptic buzz (success pattern), triggers completion
- Sparks: At completion, 8-12 small blue dots burst outward from ring (Framer variants, opacity 1→0 over 0.5s, spread 40px outward)

MeditationPlayer.tsx (for meditation exercises):
- Visual: Pulsing circle that expands/contracts with breathing pace
  - Expand (inhale): 4 seconds, circle grows from 80px to 140px
  - Hold: 2-4 seconds, circle holds size, subtle glow
  - Contract (exhale): 4-6 seconds, circle shrinks back
- Colors shift with breath: inhale=neuralBlue brightening, exhale=purple softening
- Text cue: "Breathe In..." / "Hold..." / "Breathe Out..." fading in/out
- Background: very subtle radial gradient pulsing with breath
- Timer overlaid top-right (small, 14px, text-muted)

FlashcardDeck.tsx (for RP/SR exercises):
- Stack of cards visual (3 cards stacked, front card raised, back 2 offset/dimmed)
- Front card: bg-[#2A2A2A], w-full, h-60, rounded-xl, centered text
- Question side: question text in 20px white, "Tap to reveal" hint in 12px text-muted
- Tap/click: 3D flip animation (Framer rotateY 0→180°, 0.4s, backface-hidden)
- Answer side: answer text in 20px, rating buttons below (1-5 difficulty)
  - Buttons: "Again" (red), "Hard" (orange), "Good" (green), "Easy" (blue)
  - Each button = different spaced repetition interval display ("< 1 day", "3 days", "7 days", "14 days")
- After rating: card slides out (Framer x: 300, opacity: 0, 0.3s), next card rises from stack
- Progress: "Card 5/20" counter + progress bar at top

FeynmanRecorder.tsx (for FT exercises):
- Topic prompt card at top: "Explain: {concept}" in 20px bold
- "As if to a 10-year-old" subtitle in 14px text-muted (only for relevant weeks)
- Two modes: Voice Record or Text Write (toggle tabs)
- Voice mode:
  - Mic button: 64px circle, red when recording (pulsing scale animation), gray when idle
  - Waveform visualizer: horizontal bars (20 bars, heights oscillate randomly 10-80% when recording)
  - Recording time display
  - "Stop & Analyze" button appears when recording
- Text mode:
  - Dark textarea, bg-[#2A2A2A], border-gray-700, focus:ring-neuralBlue
  - Character count / word count in corner
  - "Analyze" button (primary blue)
- AI Feedback (shown after analysis):
  - Toast/card slides in from right
  - Green check or yellow warning icon
  - Feedback text: "Good depth!" or "Gap detected: you skipped how X connects to Y"
  - Suggestions: "Try adding an analogy for {concept}"

PuzzleGame.tsx (for puzzles/games):
- Container with embedded game canvas
- For Sudoku: 9x9 grid with dark themed cells, number input, error highlighting (red flash)
- For Chess: Simple board (SVG pieces), puzzle prompt "White to move - Mate in 2"
- Timer running at top
- Difficulty indicator stars (1-5)
- Completion: score calculation + confetti

DualNBack.tsx (for N-Back training):
- Grid display: 3x3 grid of squares (each 60x60px, bg-[#2A2A2A])
- Visual stimulus: one square lights up blue for 500ms, then dims
- Audio stimulus: letter played (A-F) simultaneously
- User buttons at bottom: "Position Match" (left) + "Audio Match" (right)
- Must press within 1.5s of stimulus if it matches N positions back
- Score display: accuracy %, current N-back level
- Round counter: "Trial 15/30"
- Correct match: green flash on button + haptic
- Wrong: red shake + haptic error pattern
- Framer: grid square lighting is spring scale from 0.9→1.1→1.0 with blue glow

SpeedDrill.tsx (for speed processing):
- UFOV-style: brief flash display (200-500ms), identify target among distractors
- Full-screen flash area (dark bg), then target appears briefly
- User taps where target was (or selects from options)
- Reaction time tracked (ms display)
- Progressive difficulty: shorter flash duration, more distractors
- Score: accuracy % + average reaction time

ExerciseComplete.tsx:
- Full-screen overlay celebration
- Confetti burst (Lottie animation: blue and purple particles raining, 2s loop once)
- Large checkmark (scale from 0 to 1.2 to 1.0, green, 80px)
- "Session Complete!" in 28px bold
- Stats: duration, accuracy (if applicable), focus rating input
- Focus rating: 5 star selector (tap stars 1-5, selected = neuralBlue filled, unselected = gray outline)
- "Continue" button → returns to dashboard OR next exercise in session
- Streak update: if streak increased, flame icon bursts with "+1" floating up (Framer y: -50, opacity: 0)

=== SCREEN 4: LEVELS OVERVIEW ===

File: src/app/(app)/levels/page.tsx + components/levels/*

LevelTimeline.tsx:
- Vertical timeline with 6 level nodes
- Central vertical line: 2px, bg-gray-700, dashed
- Each node: LevelCard positioned alternating left/right (or stacked on mobile)
- Active level: line glows blue up to that point
- Completed levels: line solid green up to that point
- Locked levels: line stays gray, cards dimmed (opacity 0.5)
- Scroll into view: Framer stagger animation (each card fades up with 0.15s delay)

LevelCard.tsx:
- Card: 320x200px (mobile: full-width), bg-[#1E1E1E], rounded-xl
- Top: Level name in 20px bold + level number badge (circle, 32px, colored by status)
- Subtitle: "Weeks {X}-{Y} | {N} min/day" in 14px text-muted
- Progress bar: linear, h-2, rounded-full
  - Fill: gradient neuralBlue to creativePurple
  - Width: percentage of days completed in level
  - Particle effect: tiny dots float up from progress bar fill edge (Framer)
- Status badge bottom: "Locked" (gray), "In Progress" (blue pulse), "Completed" (green check)
- Active card: blue border-2 + glow shadow
- Tapping opens level detail: /levels/[levelId]

Level detail page (/levels/[levelId]/page.tsx):
- WeeklyTabs: 6 pill-shaped tabs (120px each), horizontal scroll, selected = neuralBlue bg + glow
- Below tabs: daily breakdown for selected week
- Each day row: Day number, 3 mini-circles (M/A/E status), total exercises count
- Tapping a day shows that day's full exercise plan (same as exercise/today but for any day)

LevelUnlockAnimation.tsx:
- Plays when user advances to a new level
- Full-screen overlay
- Lock icon centered (80px), initially intact
- Animation sequence (Framer):
  1. Lock shakes (rotate -5° to 5°, 3 cycles, 0.3s)
  2. Lock cracks (SVG path animation, crack line appears)
  3. Lock shatters (fragments fly outward, each piece: random x/y velocity + rotation + opacity→0, 0.8s)
  4. Level name fades in behind (scale 0.5→1.0, 32px bold, colored by level)
  5. Confetti burst (blue + purple + green)
  6. "Level X Unlocked!" text + "Continue" button
- Haptic: long vibration on shatter

=== SCREEN 5: AI COACH ===

File: src/app/(app)/coach/page.tsx + components/coach/*

Chat interface:

ChatBubble.tsx:
- User messages: right-aligned, bg-neuralBlue, rounded-2xl rounded-br-sm, max-w-[80%]
- Coach messages: left-aligned, bg-[#2A2A2A], rounded-2xl rounded-bl-sm, max-w-[80%]
- Text: 16px white
- Timestamp: 12px text-muted, below bubble
- Coach avatar: 32px circle, brain icon, purple ring, left of coach bubbles
- Framer: each bubble slides in from its side (x: ±30, opacity: 0→1, spring)
- Typing indicator: 3 dots pulsing (each dot delays 0.15s, scale 0.5→1.0→0.5, infinite)
  - Dots styled as tiny neural pulse circles (neuralBlue)

CoachInput.tsx:
- Fixed bottom of coach screen, bg-[#1E1E1E], border-t border-gray-800, p-3
- Input: dark bg, rounded-full, h-12, pr-14 (room for send button)
- Send button: circle 40px, creativePurple bg, send icon (arrow-up), inside input right side
- Framer: send button scales 0.9→1.0 on message send
- Quick-action pills above input: "How am I doing?", "Suggest next steps", "Why is my score low?"

SuggestionCarousel.tsx:
- Horizontal scrollable row of suggestion cards (Framer drag constraints)
- Each card: 280x100px, bg-[#2A2A2A], rounded-xl, border-l-4 (colored by type)
  - Blue border: technique suggestions ("Try adding HIIT before your afternoon session")
  - Purple border: AI insights ("Your recall accuracy peaks in the morning")
  - Green border: encouragement ("7-day streak! Your brain is rewiring")
  - Red border: warnings ("Stress index elevated — consider NSDR")
- Card content: icon + title (16px bold) + description (14px text-secondary)
- Tapping a card sends it as a message to the coach for elaboration

NSDRModal.tsx:
- Auto-triggered when readiness < 40 (Stress Shield feature)
- Modal overlay with calming purple gradient bg (opacity 0.9)
- "Your brain needs rest" heading in 24px
- "Stress Shield activated — 10 min NSDR recommended" in 16px
- Start NSDR button (green, full-width, h-14)
- "Skip" link (text-muted)
- If started: transitions to MeditationPlayer with NSDR-specific settings (slower breath, deeper relaxation cues)

=== SCREEN 6: TRADING SIMULATOR ===

File: src/app/(app)/trading/page.tsx + components/trading/*

Full implementation in Phase 4 — but build the UI shells now:

CandlestickChart.tsx:
- Full-width canvas/SVG chart area, h-80 (320px)
- Dark bg (#121212), grid lines in #1E1E1E
- Green candles (close > open): filled green rectangles + wicks
- Red candles (close < open): filled red rectangles + wicks
- X-axis: time labels | Y-axis: price labels
- Touch/mouse: pinch zoom, drag pan
- Current price line: horizontal dashed blue line
- Volume bars at bottom (20% height, semi-transparent)
- For now: render with sample data (30 candles of mock market data)

BiasAlert.tsx:
- Floating badge that appears over the chart when bias detected
- Red bg with warning icon, rounded-lg, px-3 py-2
- Text: "Endowment Effect Detected" (or other bias names)
- "Reframe?" button (small, blue)
- Framer: bounces in from top (y: -20→0, spring)
- Auto-dismisses after 5s (or on tap)

TradingScenario.tsx:
- Scenario card: bg-[#1E1E1E], rounded-xl, p-4
- Scenario description in 16px
- Decision buttons: "Buy" (green), "Sell" (red), "Hold" (blue)
- Timer: 30-second countdown for decision
- After decision: AI feedback card slides up

DecisionPanel.tsx:
- Bottom panel below chart
- Portfolio summary: Balance, P&L today, Win rate
- Quick action buttons: Buy, Sell, Hold (each 48px height)
- Position size input (slider or numeric)

TradingLeaderboard.tsx:
- Table: rank, avatar, username, EV score, win rate
- Top 3: gold/silver/bronze medal icons (spinning animation on hover)
- Current user highlighted row (neuralBlue bg)
- Sortable columns (tap header to sort)

=== SCREEN 7: COMMUNITY ===

File: src/app/(app)/community/page.tsx
- Simple forum-style list view (build UI shell — full backend in Phase 4)
- Cards: avatar (40px circle) + username + post preview + timestamp
- Create post FAB button (bottom-right, purple, + icon)
- Buddy challenge cards (green border, "Challenge {user} to N-Back!")

=== SCREEN 8: PROFILE ===

File: src/app/(app)/profile/page.tsx + components/profile/*

AvatarEditor.tsx:
- 80px circle with user initial or uploaded image
- Purple ring border (3px)
- "Edit" overlay on hover (camera icon, bg-black/50)
- Optional: neural filter toggle (add blue glow effect to avatar)

StatsCharts.tsx:
- Line charts for: Brain Score trend, Stress Index trend, Streak history
- Use Recharts or custom SVG
- Dark themed: bg-transparent, grid lines #2A2A2A, data lines neuralBlue/purple
- Tooltips: dark bg, white text, on hover/touch
- Time range selector: 7D / 30D / 90D / All (pill tabs)

SettingsPanel.tsx:
- Dark/Light toggle (custom switch, smooth transition 200ms)
- Notification toggle
- Haptic feedback toggle
- Schedule editor (morning/afternoon/evening time pickers)
- Account section: email display, change password link
- Subscription: current tier + upgrade button (if free)
- "Log Out" button (danger red variant, bottom)
- About section: version, credits

=== SCREEN 9: ONBOARDING COMPLETE → DASHBOARD FIRST VISIT ===

When user first arrives at dashboard after onboarding:
- Welcome modal: "Welcome to Level 1: Neural Ignition" with brain icon
- Brief overview card: "Your first 6 weeks focus on: meditation, recall training, exercise, and sleep"
- "Start Your First Session" button (glowing blue, pulsing)
- Dismiss reveals normal dashboard with today's 3 sessions pre-loaded

CRITICAL: Build EVERY component listed above fully. No empty returns, no "coming soon" text, no placeholder divs. Every Framer animation must be implemented. Every interactive element must work (tap, hover, focus). Test that the app compiles with `npm run build` — zero errors.
```

---